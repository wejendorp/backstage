import { createPlugin } from '@spotify-backstage/core';
import ExamplePage from './components/ExamplePage';

export default createPlugin({
  id: '{{ id }}',
  register({ router }) {
    router.registerRoute('/{{ id }}', ExamplePage);
  },
});
